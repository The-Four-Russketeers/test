from django.apps import AppConfig


class ApiConfig(AppConfig): #class that was automatically created when we created this class
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'api'
